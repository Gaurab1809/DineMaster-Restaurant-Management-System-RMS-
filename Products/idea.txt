CREATE TABLE categories (
    categorie_id INT AUTO_INCREMENT PRIMARY KEY,
    categorie_name VARCHAR(255) NOT NULL UNIQUE
);

CREATE TABLE products (
    product_id INT AUTO_INCREMENT PRIMARY KEY,
    categorie_id INT,
    product_name VARCHAR(255) NOT NULL,
    product_picture VARCHAR(255),
    product_price DECIMAL(10, 2),
    available_status BOOLEAN,
    product_description TEXT,
    FOREIGN KEY (categorie_id) REFERENCES categories(categorie_id)
);

I want "Product Management System" for "Restaurant Management System"

For this project possible links are 
index.php
db_connection.php
get_products.php
add_product.php
edit_product.php
delete_product.php
style.css (if need)
script.js (for more responsive)


i want product management system 
where i can add products according to category 
then show the table category wise product and
Edit and delete option in action column in each row